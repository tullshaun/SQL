Hi Team,

We have identified the source of the recent high CPU and disk I/O usage on the SQL Server (Orchestrade database).

From Activity Monitor, the load is being generated by ad hoc SELECT queries originating from Microsoft Office. One session in particular shows significantly high resource consumption (CPU time ~1,054,547 and Disk I/O ~547,252).

These queries are not using stored procedures and instead execute large, complex SQL statements directly against the database. The queries include multiple UNION operations and extensive string manipulation (e.g. SUBSTRING, CONCAT), which prevents efficient index usage and results in increased CPU and disk activity.

In addition, the queries repeatedly scan the same table (TBL_QUOTE) and use non-optimised filtering patterns, which further increases load on the server.

Summary of issue:
- Queries executed directly from Microsoft Office (likely Excel/Power Query or similar)
- No use of stored procedures or optimised database objects
- Non-sargable conditions due to string functions
- High CPU and disk usage as a result of repeated table scans

Recommended next steps:
- Review and optimise the query logic (ideally move into a stored procedure or view)
- Avoid string parsing within WHERE clauses where possible
- Consider indexing strategy for key columns (e.g. NAME, QUOTEDATE, UPDATETIME)
- If possible, reduce frequency or scope of data extraction from client tools

Please let me know who owns this query/report so we can work together on optimising it.

Regards, 
[Your Name]
